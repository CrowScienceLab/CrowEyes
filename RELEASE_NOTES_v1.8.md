# CrowEyes Image Viewer 1.8

## 선택형 로컬 콘텐츠 안전

- 새 설치와 업그레이드 모두 기본 OFF인 `노골적인 노출 이미지 차단` 옵션 추가
- ON일 때만 Apache-2.0 NudeNet ONNX 모델과 ONNX Runtime CPU를 지연 로드
- 이미지·경로·판정 결과를 외부 서버나 API로 전송하지 않는 완전 로컬 처리
- 판정 전 원본/썸네일을 노출하지 않으며 모델 오류 시에도 원본을 숨기는 보호 동작
- 파일 크기·수정 시각·모델·정책 버전 기반 SQLite 캐시와 캐시 삭제 버튼
- GIF/WebP 대표 프레임, PSD 합성 이미지, SVG/EPS 렌더링 결과를 제한된 크기로 판정
- 차단 화면에 약 19KB 투명 WebP 까마귀 캐릭터 사용

## NAS·대형 폴더 응답성

- 파일 연결로 연 이미지를 먼저 표시/판정한 뒤 폴더 목록 스캔
- 네트워크 왕복을 늘리던 파일별 `resolve()` 제거
- 썸네일 디코딩을 UI 스레드 밖에서 수행
- 현재 이미지 주변 ±1/±2 및 실제 보이는 목록을 우선 처리
- 안전 필터 ON에서는 동일 우선순위로 다음 이미지 판정을 미리 캐시

## 화면 및 탐색 수정

- 플레이리스트 `파일명으로만 보기`에서 실제 클릭한 파일을 여는 오류 수정
- 창 크기가 바뀔 때 화면 맞춤, 원본 크기, 사용자 확대 상태를 그대로 유지
- 첫 실행 시 플레이리스트·이미지·정보 영역이 지나치게 좁아지지 않도록 초기 비율과 최소 폭 보정
- 제목 표시줄과 EXE 아이콘을 까마귀 눈 디자인으로 교체
- 차단 상태에서도 이미지 화면 좌우 이전·다음 버튼으로 탐색 가능

## 배포 및 고지

- 로컬 모델 약 11.6MiB와 ONNX Runtime이 추가되어 Setup은 25.4MB → 43.4MB(+70.9%), Portable은 34.4MB → 61.1MB(+77.6%)로 증가
- 모델 SHA-256과 Apache-2.0/MIT 라이선스 원문 및 제3자 고지 포함
- 민감한 테스트 이미지는 저장소와 배포물에 포함하지 않음

## 기준 PC 로컬 성능 확인

- ONNX 모델 최초 준비: 약 422ms
- 1920×1080 안전한 합성 이미지 첫 판정: 약 118ms
- 동일 파일 SQLite 캐시 판정: 약 6ms

수치는 2026-09-01 빌드 PC의 단일 실행 참고값이며 PC·저장장치·NAS 상태에 따라 달라질 수 있습니다.
